apiVersion: v1
kind: Service
metadata:
  name: hola-mundo-dotnet-svc
spec:
  type: LoadBalancer
  selector:
    app: hola-mundo-dotnet
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
